# Contrato: ObservedPattern

A definição executável e versionada está em
`contracts/schemas/pattern.schema.json`.

O contrato representa um padrão observado como candidato, promovido ou
superado. `promoted` só é um estado de suporte operacional; não significa que
o padrão seja uma verdade, intenção ou tarefa executável.
